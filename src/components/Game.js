import React from "react";

function Game(props) {
  function verifyCorrectClick(x, y) {
    // Receives X and Y and
  }
  function getClick(e) {
    // Gets coordinates X and Y of where the person clicked.
  }
  return <div>Game</div>;
}

export default Game;
